# ifstream.test
This repos contains a file where i tested with "std::ifstream" where i could input a file outside of my c++ compiler and display it on an output
the syntax i used has only been used on .txt and .json files where i store the contents of the file into a vector and extracted the chars of the strings inside the vector inside their own vectors
still a beginner programmer here
